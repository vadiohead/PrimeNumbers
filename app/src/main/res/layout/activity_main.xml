<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#dcdcdc"
    tools:context=".MainActivity">

    <!-- Spinner -->
    <Spinner
        android:id="@+id/spinner"
        android:layout_width="200dp"
        android:layout_height="50dp"
        android:layout_marginStart="20dp"
        android:layout_marginTop="10dp"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- Prime Layout (using ConstraintLayout instead of LinearLayout) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/prime_layout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:visibility="gone"
        app:layout_constraintTop_toBottomOf="@id/spinner"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Definition TextView -->
        <TextView
            android:id="@+id/definition"
            android:layout_width="329dp"
            android:layout_height="67dp"
            android:text="@string/definition"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- TextView 2 -->
        <TextView
            android:id="@+id/textView2"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="40dp"
            android:text="@string/text"
            app:layout_constraintTop_toBottomOf="@id/definition"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- From TextView -->
        <TextView
            android:id="@+id/from"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="40dp"
            android:layout_marginTop="30dp"
            android:text="@string/from"
            app:layout_constraintTop_toBottomOf="@id/textView2"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- From Value EditText -->
        <EditText
            android:id="@+id/fromValue"
            android:layout_width="162dp"
            android:layout_height="48dp"
            android:ems="10"
            android:inputType="number"
            android:text="2"
            app:layout_constraintTop_toTopOf="@id/from"
            app:layout_constraintStart_toEndOf="@id/from"
            android:layout_marginStart="21dp"
            android:layout_marginTop="-20dp"/>

        <!-- To Value EditText -->
        <EditText
            android:id="@+id/toValue"
            android:layout_width="162dp"
            android:layout_height="48dp"
            android:ems="10"
            android:inputType="number"
            android:text="1000"
            app:layout_constraintTop_toBottomOf="@id/fromValue"
            app:layout_constraintStart_toEndOf="@id/from"
            android:layout_marginStart="21dp"/>

        <!-- To TextView -->
        <TextView
            android:id="@+id/to"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="40dp"
            android:layout_marginTop="30dp"
            android:text="@string/to"
            app:layout_constraintTop_toBottomOf="@id/from"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- Button Container (LinearLayout replaced with ConstraintLayout) -->
        <androidx.constraintlayout.widget.ConstraintLayout
            android:id="@+id/buttonContainer"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            app:layout_constraintTop_toBottomOf="@id/toValue"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHorizontal_bias="0.5">

            <!-- Calculate Button -->
            <Button
                android:id="@+id/calculate"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:onClick="calculate"
                android:text="@string/calculate"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent" />

            <!-- Copy Button -->
            <Button
                android:id="@+id/copy"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:onClick="copyToClipboard"
                android:text="@string/copy"

                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/calculate" />

        </androidx.constraintlayout.widget.ConstraintLayout>

        <!-- ScrollView for results -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:background="@color/black"
            app:layout_constraintTop_toBottomOf="@id/buttonContainer"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- Numbers TextView -->
                <TextView
                    android:id="@+id/numbers"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:fontFamily="monospace"
                    android:text="@string/display"
                    android:textColor="#228b22"/>
            </LinearLayout>
        </ScrollView>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Divisor Layout (Visibility Gone initially) -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:id="@+id/divisor_layout"
        android:layout_width="match_parent"
        android:layout_height="0dp"
        app:layout_constraintTop_toBottomOf="@id/spinner"
        app:layout_constraintBottom_toTopOf="@id/bottom_navigation"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Denominator Definition -->
        <TextView
            android:id="@+id/divisor_definition"
            android:layout_width="329dp"
            android:layout_height="67dp"
            android:text="@string/divisor_definition"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- From TextView -->
        <TextView
            android:id="@+id/number_input_text"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="40dp"
            android:layout_marginTop="30dp"
            android:text="@string/enter_number"
            app:layout_constraintTop_toBottomOf="@id/divisor_definition"
            app:layout_constraintStart_toStartOf="parent"/>

        <!-- From Value EditText -->
        <EditText
            android:id="@+id/number_input"
            android:layout_width="162dp"
            android:layout_height="48dp"
            android:ems="10"
            android:inputType="number"
            android:text="2"
            app:layout_constraintTop_toTopOf="@id/number_input_text"
            app:layout_constraintStart_toEndOf="@id/number_input_text"
            android:layout_marginStart="21dp"
            android:layout_marginTop="-20dp"/>

        <!-- Calculate Divisors Button -->
        <Button
            android:id="@+id/calculate_divisors"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/calculate_divisors"
            app:layout_constraintTop_toBottomOf="@id/number_input"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintEnd_toEndOf="parent"/>

        <!-- ScrollView for Divisor Results -->
        <ScrollView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:background="@color/black"
            app:layout_constraintTop_toBottomOf="@id/calculate_divisors"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintStart_toStartOf="parent">

            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- Numbers TextView -->
                <TextView
                    android:id="@+id/divisor_result"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:fontFamily="monospace"
                    android:text="@string/display"
                    android:textColor="#228b22"/>
            </LinearLayout>
        </ScrollView>
    </androidx.constraintlayout.widget.ConstraintLayout>

    <!-- Bottom Navigation -->
    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottom_navigation"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:menu="@menu/navigation_menu"/>

</androidx.constraintlayout.widget.ConstraintLayout>